name: rexios
description: A simple bootstrap utility for all of the CLI tools I daily drive
version: 1.0.0

environment:
  sdk: ">=2.19.0 <3.0.0"

dev_dependencies:
  rexios_lints: ^5.0.0

executables:
  rex: rex
